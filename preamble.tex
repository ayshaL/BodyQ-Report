% ── Packages ──────────────────────────────────────────
\usepackage[T1]{fontenc}
%\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{geometry}
\geometry{a4paper, margin=2.5cm}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{parskip}
\usepackage{tocloft}
\usepackage{appendix}
\usepackage{tabularx}
\usepackage{booktabs} % This fixes the 'Undefined control sequence' in tables
\usepackage{enumitem}
\usepackage{amssymb} % This defines \checkmark
\usepackage{xltabular}

\usepackage[htt]{hyphenat}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, shapes.multipart, arrows.meta, positioning, calc, fit, backgrounds}

\tikzset{
	sdactor/.style={
		rectangle, draw, thick, fill=blue!10,
		minimum width=2.1cm, minimum height=0.6cm,
		align=center, font=\scriptsize\bfseries
	},
	sdlife/.style={dashed, gray!70, line width=0.5pt},
	sdact/.style={
		rectangle, draw, fill=blue!30, line width=0.5pt,
		minimum width=4pt
	},
	sdmsg/.style={->, thick, >=Triangle, font=\scriptsize},
	sdret/.style={->, thick, >=Triangle, dashed, font=\scriptsize},
	sdnote/.style={
		rectangle, draw=gray!50, fill=yellow!15,
		font=\scriptsize, align=center, inner sep=2pt
	},
	sdfrag/.style={
		rectangle, draw=gray!60, thick,
		font=\scriptsize\bfseries, inner sep=4pt
	},
	sdphase/.style={
		rectangle, draw=gray!50, fill=gray!10, thick,
		font=\scriptsize\bfseries, inner sep=2pt
	}
}

\usepackage{amsmath}
\usepackage[nottoc]{tocbibind}

\addto\captionsenglish{\renewcommand{\bibname}{References}}


\onehalfspacing

% ── Header / Footer ───────────────────────────────────
\pagestyle{fancy}
\setlength{\headheight}{14.5pt}
\fancyhf{}
\rhead{ISS 396 -- Junior Project}
\lhead{BodyQ}
\cfoot{\thepage}
